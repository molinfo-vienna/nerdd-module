[pytest]
log_cli = 1
; log_cli_level = INFO
addopts = -x -s --cov-report term --cov=nerdd_module
; add '-m "wip"' to focus on tests marked with "wip" (@pytest.mark.wip)
markers = 
    wip: marks tests as work in progress

[pytest-watch]
ext=py,feature